-- Posture Check! - Initial Database Setup Placeholder
-- Note: The complete database schema, tables (profiles, sessions, posture_events, streaks),
-- and Row Level Security (RLS) policies will be created in Phase 4 (Step 4.2).

-- Enable standard required extensions
CREATE EXTENSION IF NOT EXISTS "uuid-ossp";
CREATE EXTENSION IF NOT EXISTS "pgcrypto";
